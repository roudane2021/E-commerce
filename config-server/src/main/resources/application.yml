server:
  port: 9101

spring:
  cloud:
    compatibility-verifier:
      enabled: false
    config:
      server:
        git:
          uri: https://github.com/roudane2021/config-server-repo.git
  application:
    name: config-server

